# Mobile app (Expo)

Команды:
- npm run start
- npm run android
- npm run ios

Перед запуском установите зависимости в корне проекта.

Env:
- скопировать .env.example → .env и указать EXPO_PUBLIC_SUPABASE_URL / EXPO_PUBLIC_SUPABASE_ANON_KEY
