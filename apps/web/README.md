# Web app (Next.js)

Команды:
- npm run dev
- npm run build
- npm run start

Перед запуском установите зависимости в корне проекта.

Env:
- скопировать .env.example → .env.local и указать NEXT_PUBLIC_SUPABASE_URL / NEXT_PUBLIC_SUPABASE_ANON_KEY
